li
    .collapsible-header
        i.material-icons settings
        | #{__('settings.general.title')}
    .collapsible-body
        span #{__('settings.general.classActive')}
        .switch
            label
                | #{__('switch.off')}
                input(type='checkbox', name='classActive' , checked=generalActive)
                span.lever
                | #{__('switch.on')}